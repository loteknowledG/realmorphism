import { KitShowroom } from "./kit/kit-showroom.tsx";

/** Vite demo entry — same kit as Echo Mirage with local form-control samples. */
export function DemoShowroom() {
  return <KitShowroom showFormControls badgeLabel="Realmorphism Registry" />;
}
